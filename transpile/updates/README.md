# Upstream reconciliation intervals

Each JSON file is a finite generated review shell for one canonical  interval. Generated facts are not hand-edited; semantic annotations are completed before code changes and the pin moves only after the interval closes.

The first batch ends at  and contains at most twenty first-parent upstream commits. It is the first slice of the larger bounded catch-up toward .
